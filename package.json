{
  "name": "maxtodonte2",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "git checkout main && git pull && npm i && vite build && sudo rm -r /var/www/1max2splits/* && sudo cp -r dist/* /var/www/1max2splits",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "svelte": "^5.28.1",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "fast-xml-parser": "^5.2.3"
  }
}
